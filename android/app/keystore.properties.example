# Copy this file to android/app/keystore.properties and fill in your own real values.
# keystore.properties (the real one, not this .example) is gitignored -- never commit it.
#
# storeFile is relative to the android/ project root (where settings.gradle lives).
storeFile=../ignyt-release.jks
storePassword=
keyAlias=ignyt
keyPassword=
